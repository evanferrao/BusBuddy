/**
 * Passenger Home - Dashboard
 * 
 * Main screen for passengers showing:
 * - Bus tracking
 * - Wait for Me button
 * - Absent Today button
 * - Stop color status
 */

import NewPassengerDashboard from '@/components/new-passenger-dashboard';
import React from 'react';

export default function PassengerHomeScreen() {
  return <NewPassengerDashboard />;
}
